rules_version = '2';

// 각 사용자는 자신의 데이터(users/{uid}/**)만 읽고 쓸 수 있습니다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
